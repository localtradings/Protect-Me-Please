# Skills

Run:

```sh
breachproof skill export --codex
```

The export command creates a Codex-compatible skill pack with safety boundaries, tool usage guidance, report schema notes, and safe workflows.
